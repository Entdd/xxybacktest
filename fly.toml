app = "xxybacktest"
primary_region = "nrt"

[build]
  dockerfile = "Dockerfile"

[[vm]]
  memory = "256mb"
  cpu_kind = "shared"
  cpus = 1

[[services]]
  internal_port = 5000
  protocol = "tcp"
  auto_stop_machines = false

  [[services.ports]]
    port = 80
    handlers = ["http"]
